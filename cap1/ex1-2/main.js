// Recebendo o nome do usuário
const nome = prompt("Qual é o seu nome?");

// Comprimentando o usuário
alert(`Olá, ${nome}`); // Essa é uma template string

// Outra forma de exibir a mesma mensagem acima: alert("Olá" + nome);